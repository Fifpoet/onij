# 复制为 .env.production 后执行 pnpm build（变量会打进前端包，勿写密钥）
# 已提供 .env.production：VITE_SERVER_URL=http://onij.fun:8889
# 本地开发用 .env.development（127.0.0.1:8889），切勿在 .env 里写 127.0.0.1
# 子路径部署时取消注释并设路径，末尾须有 /
# VITE_PUBLIC_PATH=/app/
# 生产环境取播放直链；不填则默认 https://music-api.gdstudio.xyz
# VITE_GDSTUDIO_MUSIC_URL=https://music-api.gdstudio.xyz
