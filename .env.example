# 复制为 .env / .env.development 后本地开发
# 主站 Go API（本地 go run . 默认 :8889）
VITE_SERVER_URL=http://127.0.0.1:8889
# 网易云 HTTP 代理（统一走线上）
VITE_NETEASE_URL=http://onij.fun:3000/
# 本地 cloud.onij.fun 无法解析时：hosts 添加 cloud.onij.fun，或云盘走 /file/preview（默认）
# 勿将 QINIU_PUBLIC_DOMAIN 设为 http://onij.fun（主站 nginx 不提供 /cloud 对象路径会 404）

# 生产构建请使用 .env.production（参考 .env.production.example）
