# torch 走 CUDA 索引；onnxruntime-gpu 在 PyPI，必须用 extra-index-url 不能只用 index-url
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
onnxruntime-gpu==1.20.2
-r requirements-base.txt
# faster-whisper 会顺带装 CPU onnxruntime，装完后卸掉以免覆盖 GPU 包
# 由 run.bat / repair_torch.bat 执行: pip uninstall -y onnxruntime