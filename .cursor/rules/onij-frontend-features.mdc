---
description: 播放器、MV、练习页等前端功能约定（非云盘）
globs: src/views/**,src/components/**,src/store/**
alwaysApply: false
---

# 前端功能约定（非云盘）

## MV（歌曲详情）
- `mvUrl` **持久化**到后端；播放前通过接口批量/单条获取是否有 MV。
- **不做 B 站 iframe 内联**（跨域无法可靠监听播放结束、音量/seek 问题多）；改为输入 URL 后**新窗口/跳转打开**。
- MV 输入：单行气泡状输入框；**失焦或 Enter 保存**；前端校验 URL 合法性。
- 歌曲名旁小电视图标：无 MV 时点击输入，有 MV 时高亮可跳转。

## 播放队列
- 专辑页提供 **「全部播放」**，按顺序加入队列。
- 队列页有 **置顶** 按钮。
- **双击播放其他歌时，当前正在播放的歌不要加回队列**。

## 练习页（Prac）
- 不做顶部统计卡片；改为 **「月度统计」按钮**，点击后在右侧卡片展示。
- 支持按 **练习类型** 维度切换；展示 **时长折线图**。
- 日历：一种练习类型在日期数字下显示 **小实心三角**；两种及以上显示 **小实心圆**。
- 月概览可切换展示模式；日历格可有热力底色。

## 其他 UI
- 根页面（App/布局）底部展示备案号：**鄂ICP备2024057031号**。
- 修改无关页面（如搜索页 `HeaderSearch`、搜索结果样式）时保持原样，禁止顺带改版。

## 依赖与 EXIF
- 图片 EXIF 用项目内 `src/util/exif.ts`（轻量自研），不要强依赖 `exifr`（安装曾失败）。
