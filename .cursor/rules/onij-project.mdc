---
description: onij 项目通用约定：开发环境、对话工作模式、部署与改动范围
alwaysApply: true
---

# onij 项目通用约定

## 语言与沟通
- 与用户沟通使用**简体中文**。
- 未经明确要求，不要主动创建 commit / push / PR。

## 开发环境
- **本地**：Windows + 高性能单张显卡；日常开发与测试在本机完成。
- **线上**：Linux 云服务器（`117.72.33.136`，域名 `onij.fun`）。
- **主站 Go API（本地）**：`VITE_SERVER_URL=http://127.0.0.1:8889`（`.env.development`）。
- **网易云代理**：本地也固定走线上 `VITE_NETEASE_URL=http://onij.fun:3000/`，不要改成本地代理。
- **生产构建**：`VITE_SERVER_URL` 留空或填 `https://onij.fun/`；**禁止**在公网前端包里写 `127.0.0.1`。
- 线下开发时主站接口必须调本地 Go；第三方代理（网易）仍走线上。

## 对话工作模式（重要）
用户每次对话会（或应）标明当前模式；**未标明时根据意图自行判断**，并默认按最保守方式执行。

| 模式 | 含义 | Agent 行为 |
|------|------|------------|
| **讨论** | 探讨方案、排错思路 | **不修改仓库代码**；最多给出思路、伪代码或示例片段（不直接落盘改文件） |
| **测试性修改** | 本地验证 | 可改代码并在**本地测试环境**验证（本机 Go `:8889` + Vite dev）；**不** commit / push / SSH 上线 |
| **部署上线** | 发布生产 | 本地构建 → git push → 服务器 `git pull` → 重启 `onij-server` → 同步静态资源 → `nginx reload` → 验证 |

**自行判断参考**：
- 问「为什么」「怎么做」「帮我看看」→ 讨论
- 说「改一下」「试试」「本地跑一下」→ 测试性修改
- 说「部署」「上线」「发布到线上」→ 部署上线
- 不确定时：先讨论或本地改，**不要**主动 SSH / push

## 排错与部署
- 非「部署上线」模式：**不要**为排查问题主动 SSH 登录服务器。
- SSH 凭据：使用本机环境变量 **`sshpwd`**；**禁止**把密码写入 rules、代码、commit 或日志。
- 需要 SSH 时通过 `$env:sshpwd`（PowerShell）引用，勿 echo 明文密码。
- 二进制部署后若启动失败，检查 `chmod +x release/bin/onij-server`。

## 改动范围（重要）
- **保持改动范围可控**：只改与任务直接相关的文件，不顺手重构无关模块。
- 曾发生过云盘改动误伤搜索页样式；修改 `App.vue`、布局、全局样式时务必最小 diff。
- 清理残余代码时，只删与任务相关的残留，不恢复/重写无关页面。

## 部署与静态资源
- 前端产物：`pnpm build:release:win`（或 `build:release`）→ `release/web/`。
- 服务器 nginx 静态根目录：`/var/www/onij/web`；`git pull` 后需确认文件已更新。
- 线上静态资源 403 时检查目录权限（目录需 `755`，如 `assets/` 缺 `+x` 会 403）。
- 可用 `scripts/remote_deploy.py` 执行完整发布（仅「部署上线」模式）。

## 数据库 DDL
- `server/model/ddl/` **只放表结构定义**；不要在此目录加业务脚本或杂项文件。
- 需要改表时：**先告知用户**，由用户决定是否在库上执行，不要擅自加 migration 杂项。
